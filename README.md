Just create python enviroment.
 ```
 conda create --name <name>
 ```

 Activate enviroment and install pip:
```
conda activate <name>
conda install pip
```

Install requirements:
```
pip install -r requirements
```

Start the server:
```
streamlit run main.py
```